function Navbar({ currScore, maxScore, resetGame }) {
  return (
    <>
      {/* Display reset game button using resetGame function and
  displays main logo, curr and max score on navbar */}
    </>
  );
}

export default Navbar;
